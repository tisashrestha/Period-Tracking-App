name: my_flutter_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.4   # Downgraded version
  intl: ^0.18.1
  table_calendar: ^3.0.9
  provider: ^6.0.0
  google_sign_in: ^6.2.2
  firebase_core: ^2.32.0
  firebase_auth: ^4.17.6
  cloud_firestore: ^4.17.5
  tflite_flutter: ^0.11.0
  flutter_datetime_picker: ^1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/login.png
    - assets/mainpage.PNG
    - assets/signup.png
    - assets/logoo.png
